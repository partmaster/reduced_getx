name: reduced_getx
description: reduced for getx.
version: 0.1.0
homepage:
publish_to: none

environment:
  sdk: '>=2.17.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.5
  reduced:
    git:
      url: https://github.com/partmaster/reduced.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
